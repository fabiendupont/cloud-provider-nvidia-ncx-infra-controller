apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nico-ccm.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nico-ccm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "nico-ccm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nico-ccm.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "nico-ccm.fullname" . }}
      hostNetwork: true
      containers:
        - name: cloud-controller-manager
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /nico-cloud-controller-manager
            - --cloud-provider=nico
            - --cloud-config=/etc/kubernetes/cloud-config
            - --use-service-account-credentials=true
            - --leader-elect=true
            - --leader-elect-resource-name={{ include "nico-ccm.fullname" . }}
            - --v={{ .Values.logLevel }}
          volumeMounts:
            - name: cloud-config
              mountPath: /etc/kubernetes
              readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 10258
              scheme: HTTPS
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
      volumes:
        - name: cloud-config
          secret:
            secretName: {{ include "nico-ccm.secretName" . }}
            items:
              - key: cloud-config
                path: cloud-config
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
